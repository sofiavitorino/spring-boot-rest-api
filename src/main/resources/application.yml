spring:
  application:
    name: spring-boot-rest-api
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_rest_api
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true